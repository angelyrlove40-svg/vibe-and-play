# @vibemud/native-win32-arm64-msvc

Native VibeMUD binaries for `aarch64-pc-windows-msvc`.

This package is installed as an optional dependency of `vibemud`; users normally do not install it directly.
